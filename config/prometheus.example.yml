global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: sentinelgate
    metrics_path: /metrics
    authorization:
      type: Bearer
      credentials_file: /run/secrets/sentinelgate-monitor-token
    static_configs:
      - targets: ["gateway:8000"]
